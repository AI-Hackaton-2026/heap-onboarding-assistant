// Placeholder API route for AI course generation.
// Future responsibility:
//   Receive role/project input → use knowledge base + project graph
//   → generate modules, lessons, checklist, and quizzes → store generated course.

export async function POST() {
  return Response.json({ message: "Course generation placeholder" });
}
