// Placeholder API route for project CRUD.
// Future responsibility: create and list projects (name, description,
// github repo, slack workspace, status) scoped to the caller's organization.

export async function GET() {
  return Response.json({ message: "Project API placeholder" });
}

export async function POST() {
  return Response.json({ message: "Project API placeholder" });
}
